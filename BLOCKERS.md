# BLOCKERS.md — What the build needs from humans

The loop adds precise entries here when it hits something only a human can provide, then continues with other work. Humans resolve an item by editing it to start with `RESOLVED:` and describing where the credential/data lives (never paste secrets here — put them in the env/secret store and say so). See BUILD-LOOP.md for the pre-known list.

Format per entry:
- **[OPEN|RESOLVED] <short name>** — needed for §13 step X.Y · what exactly · how to provide it

---

- **[OPEN] Anthropic API key** — needed for §13 step 1.3+ (skill runner real model calls) · The runner is built and tested against `MockModelClient` (PROVISIONAL, clearly labelled in output); `AnthropicModelClient` is implemented (`server/src/skills/model.ts`, official SDK, structured outputs, fast tier `claude-haiku-4-5` / top tier `claude-opus-4-8`) but has never made a live call · Provide: create a dev-workspace key at console.anthropic.com, set `ANTHROPIC_API_KEY` in the server environment (never commit it). The runner auto-switches to the real client when the variable is present. Resolving unblocks: live hello-world run, then email-triage (1.5) drafting on real inputs.

- **[OPEN] Gmail OAuth for support@** — needed for §13 step 1.5 (live mail in/out) · The W2 pipeline is built behind `MailSender`/ingest interfaces with a PROVISIONAL recording mock (`server/src/inbox/connectors.ts`); watch-channel renewal, history-diff pull and fallback polling (SPEC-INBOX §4) are NOT yet implemented — they only make sense against the real API · Provide: Google Cloud project + OAuth consent + Gmail API credentials for a delegated account on the support@ group, send-as support@ enabled; put client id/secret + refresh token in the server env (never in the repo) · Unblocks: real ingest, send-as threading, watch/poll/reconciliation build-out. **On wiring: set `INBOX_MODE=shadow` — triage must not act on live mail (see triage-golden-set).**

- **[OPEN] ActiveCollab API token** — needed for §13 step 1.5 (task creation in AC) · Built behind `TaskConnector` with PROVISIONAL `MockActiveCollab` (`mock-ac-*` refs recorded in `tasks.external_ref`) · Provide: an AC API token + the project mapping (client → AC project id) in the server env · Unblocks: real task creation + the completion webhook registration.

- **[OPEN] Voice vendor trial accounts + one AU test number** — needed for §13 step 2.0 (voice runtime × STT × TTS latency spike) · The spike is a *vendor-selection decision* measured on real phone calls (p50/p95 first-audio, barge-in, EN⇄中文 code-switch, AU media region) — it cannot be mocked, so Stage 2 (2.0–2.6) is blocked until it lands · Provide: trial accounts for the candidate managed voice-agent runtime + STT/TTS vendors, and one Australian test DID to dial · Unblocks: the whole Receptionist product. **Per BUILD-LOOP.md rule 7, the loop skipped Stage 2 and built the next unblocked step (3.1 notification center); it will return to 2.0 when this resolves.**

- **[OPEN] Team triage golden set (Ish's 150 labelled emails)** — needed before email-triage leaves shadow mode (SPEC-INBOX §6, TEAM-TASKS.md) · The skill, eval harness and metrics (type accuracy / split recall / complaint recall) are built; only a PROVISIONAL 10-case synthetic set exists (`skills/email-triage/v1/golden.provisional.json`) — **email-triage stays in shadow mode on live mail regardless of other blockers until the real set scores ≥95% type accuracy, 100% split recall, 100% complaint recall** · Provide: 150 labelled historical emails (type, splits, urgency) as JSON in the same shape as the provisional file, incl. the hard 30.

- **[OPEN] review-summariser golden set (Wally's past Online Presence Reviews)** — needed before review-summariser leaves shadow (§13.2 step 1.10, §12.4) · The skill, its grounding validator and the eval scaffold are built; only a PROVISIONAL 10-case synthetic set exists (`skills/review-summariser/v1/golden.provisional.json`), and per BUILD-LOOP.md a skill never goes live on a synthetic exam. The validator only checks what a validator *can* check without a judge — that no number, percentage or domain appears that was not in the accepted findings. **Whether the paragraph reads like Wally, picks the right things to lead with, and is worth sending is exactly what the synthetic set cannot score.** · Provide: 20-30 past reports as {accepted findings} -> {the opening paragraph and overall comment he actually wrote}, so the set can be scored pairwise against the human original · Unblocks: review-summariser at G1 on live reviews. Until then it runs against `MockModelClient` in tests and its output must be treated as a draft a human reads before the report goes out.

- **[OPEN] Meta Page Public Content Access (not just a token)** — needed for real social metrics on *prospects* (§13.2 step 1.11) · A Graph API user token was supplied and **tested live on 2026-08-04**: it authenticates fine and returns pages the user administers (`/me/accounts` → 1 page), but all three practice pages tried returned `(#100) Object does not exist, cannot be loaded due to missing permission or reviewable feature`. Reading an arbitrary public page has required **Page Public Content Access** since 2018, which means App Review **plus** Business Verification, and Meta grants it narrowly. **A token alone does not unblock this step — do not assume supplying another one will.** · What the token *does* unblock today: 20-80's own pages, and any client page that grants the app access during onboarding — `MetaGraphProvider` already reads those and is wired in whenever `META_GRAPH_TOKEN` is set · Until PPCA: a prospect's page yields **no signal at all** (not a zero, not a guess), `{{fans}}`/`{{followers}}`/`{{posts}}` stay unfilled, and the reviewer fills the Social Media section by hand exactly as the template's own margin comments describe · Scraping is not a fallback: it breaches both platforms' terms and a report built on that is not one 20-80 can send.

- **[OPEN] SERP/Places provider decision (reopens a closed decision)** — needed for the automatic half of Competition (§13.2 step 1.12) · §13.2 recorded **free/self-hosted sources only on 2026-08-03**, which is exactly why Competition was scoped as guided manual entry. This step does **not** override that — it builds the manual path (which had never existed) and puts a `SerpProvider` interface behind it so the decision is reversible with an adapter · The stand-in is `NoSerpProvider`, which returns **nothing on purpose**: a mock that invented a rank would print "#1 in Google search" beside a named competitor in a client's report, which is the most defamatory-adjacent thing this system could fabricate · Provide (if the decision is reopened): a SERP/Places API account and key — SerpAPI or DataForSEO, roughly USD $50-100/mo at audit volumes · Unblocks: `serp_position`, `map_position`, `review_count`, `review_rating` and the SEM flag in comp.row. Until then a reviewer types them, which is what the template's own margin comments already describe ("Use SEMRush to determine whether Google Ads are running").

- **[OPEN] Google Maps Platform key (Places API)** — needed for Reputation and automatic competitor discovery (§13.2 step 1.14) · **This is the highest-value credential outstanding.** Measured against the 17 real reports in `Downloads/presence`: the Google review line ("44x 5.0* on GMB") appears in **15 of 17**, and a competitor set in **15 of 17** · One key covers both — Text Search finds the practice, Place Details returns `rating` and `user_ratings_total`, Nearby Search returns same-trade practices within 5km, which is what a competitor set is · `GooglePlacesProvider` is built and tested; set `GOOGLE_MAPS_API_KEY` and collection starts researching. Without it `NoPlacesProvider` returns nothing — never a plausible rating, because a fabricated "4.7 stars from 23 reviews" about a real practice, or a competitor that does not exist, would print on 20-80 letterhead · Cost: Places is billed per request with a monthly free allowance that comfortably covers audit volumes · Note this reopens the free-sources-only decision of 2026-08-03, the same as the SERP entry.
